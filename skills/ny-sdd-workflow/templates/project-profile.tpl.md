# 项目级配置

## 项目铁律（Constitution）
> 以下约定 AI 绝对不能违反，无论任何情况。违反时必须立即停止并报告。

### 技术约束
- [ ] {例：必须使用 Vue3，禁止使用 Vue2}
- [ ] {例：所有接口调用必须封装在 service 层，禁止在组件里直接调用}

### 代码规范
- [ ] {例：代码注释必须使用中文}
- [ ] {例：所有异步操作必须有错误处理}

### 业务约束
- [ ] {例：涉及金额的字段必须使用 Decimal，禁止使用 Float}
- [ ] {例：用户数据操作必须有操作日志}

## 技术栈
- frontend: {vue3 / react / ...}
- frontend-root: {src/ / frontend/ / ...}
- backend: {java-springboot / go / ...}
- backend-root: {server/ / backend/ / ...}
- database: {mysql / postgresql / ...}

## 外部依赖（API 学习记录）
- ...

## PRD 内容索引（AI 从 .docs/prd/ 全部文件解析）
> 设计和编码时必须以 PRD 内容为准。
> 内容类别说明：需求文档（md/pdf）/ 原型图（prototype/）/ UI 设计稿（ui/）/ UI 解析（ui-spec/）。
> 编码时 UI 基准优先级：UI 解析 md > UI 设计稿 > 原型图 > HTML 原型 > DES > frontend-context（详见 §3.1.4）。

| 文件 | 格式 | 内容类别 | 对应页面/功能 | 内容摘要 |
| --- | --- | --- | --- | --- |
| {.docs/prd/文件名} | {md/png/pdf/...} | {需求文档/原型图/UI 设计稿/UI 解析} | {页面名/功能名} | {核心内容} |

## 业务架构（AI 从代码反推，人工确认，仅旧项目）

### 业务模块划分
| 模块 | 职责 | 核心实体 | 对外接口数 |
| --- | --- | --- | --- |
| {模块名} | {职责描述} | {实体列表} | {数量} |

### 模块依赖关系
{模块A} → {模块B}（{依赖原因}）

### 角色权限总览
| 角色 | 可执行操作 |
| --- | --- |
| {角色} | {操作列表} |

## 业务流程（AI 从代码反推，人工确认，仅旧项目）

### {流程名}
- 正常路径：{主流程}
- 异常路径：{异常处理}
- 涉及模块：{模块列表}
- 关键状态流转：{状态A→状态B→...}
