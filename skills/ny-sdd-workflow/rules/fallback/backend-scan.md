# 后端内置扫描（兜底）

> 仅在后端 context skill 不可用且用户选择"使用内置兜底"时执行。
> 产出写入 `.project/specs/rules/backend-context.md`。

---

## 扫描目录与内容

| 扫描目录                           | 提取什么                                              |
| ---------------------------------- | ----------------------------------------------------- |
| utils/、common/、lib/              | 公共工具与封装（DTO转换/分页/安全认证）              |
| config/、infrastructure/           | 基础设施封装（Redis/MQ/OSS/ES/定时任务）              |
| middleware/、interceptor/、filter/ | 全局中间件/拦截器                                     |
| aspect/、aop/                      | AOP 切面（操作日志/性能监控/数据权限）                |
| mapper/、service/（基类）          | 数据库通用封装（BaseMapper/自动填充/逻辑删除/多租户） |
| security/、auth/                   | 安全/认证工具（JWT/加解密）                           |
| constants/、enums/                 | 常量与枚举（错误码/状态枚举）                         |
| entity/、model/、migration/        | 数据模型概览（实体/关系/字段约束）                    |
| controller/、service/ 返回格式     | 项目内通用模式（返回格式/参数校验/日志方式）          |

## 记录格式要求

每项公共能力写入 backend-context.md 时必须包含三要素——路径、用法、禁止：

```
- {能力名}：`{文件路径}`
  用法：`{import 语句}; {调用示例}`
  禁止：{不允许的替代做法}
```

## backend-context.md 输出结构

```markdown
# 后端项目上下文

## 构建与运行
- 安装依赖：{命令}
- 启动开发：{命令}
- 构建生产：{命令}
- 运行测试：{命令}
- 运行单个测试：{命令}

## 项目架构
- 架构模式：{DDD 分层 / MVC / ...}
- 入口文件：{Application.java}
- 路由/接口注册：{注解扫描 / 集中注册}
- 数据库访问：{ORM / MyBatis / 原生 SQL}

## 项目规范
- 命名约定：...
- 目录结构：...
- 代码风格：...

## 内部公共能力
> 编码时必须复用已有能力，禁止重复封装

### 公共工具与封装
{按三要素格式列出}

### 基础设施封装
- 缓存：{路径 + key 规范 + 过期策略}
- 消息队列：{路径 + 序列化 + 重试}
- 文件存储：{路径}
- 定时任务：{注册方式}

### 全局中间件/拦截器
{中间件名 + 用途}

### AOP 切面
{切面名 + 用途}

### 常量与枚举
- 错误码：{路径 + 编号规则}
- 状态枚举：{路径}
- 全局常量：{路径}

### 数据库通用封装
- BaseMapper / BaseService：{说明}
- 自动填充：{说明}
- 逻辑删除：{说明}
- 多租户：{说明}

### 数据模型概览
- 核心实体：{列表}
- 关键关系：{说明}
- 字段约束惯例：{说明}

### 项目内通用模式
- 接口返回格式：{说明}
- 参数校验方式：{说明}
- 日志打印方式：{说明}
- 事件机制：{说明}

## 禁止事项
- ❌ {禁止行为}（统一用 {替代方案}）
```
